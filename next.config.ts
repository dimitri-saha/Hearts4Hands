import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Nothing to override: the defaults are right for this app. Kept as a
  // placeholder so future settings have an obvious home.
};

export default nextConfig;
